# 2. 품목관리 페이지 상세 체크 보고서

## 📋 페이지 정보
- **경로**: `/master/items`
- **파일**: `src/app/master/items/page.tsx`

---

## ✅ 필터 라벨 확인

### 1. 검색 필터
- ⚠️ **라벨 없음** - placeholder만 사용 중
  - 현재: `placeholder="품목코드, 품목명, 규격, 소재로 검색..."`
  - 개선 필요: 명시적인 라벨 추가

### 2. 분류 필터
- ✅ **라벨 있음** - `sr-only` 클래스 사용
  - `<label className="sr-only" htmlFor="category-filter">분류 필터</label>`
  - 접근성은 충족하나, 시각적 라벨 없음

### 3. 타입 필터
- ✅ **라벨 있음** - `sr-only` 클래스 사용
  - `<label className="sr-only" htmlFor="item-type-filter">타입 필터</label>`
  - 접근성은 충족하나, 시각적 라벨 없음

### 4. 소재 필터
- ✅ **라벨 있음** - `sr-only` 클래스 사용
  - `<label className="sr-only" htmlFor="material-type-filter">소재 필터</label>`
  - 접근성은 충족하나, 시각적 라벨 없음

### 5. 차종 필터
- ✅ **라벨 있음** - `sr-only` 클래스 사용
  - `<label className="sr-only" htmlFor="vehicle-filter">차종 필터</label>`
  - 접근성은 충족하나, 시각적 라벨 없음

### 6. 거래처 필터
- ✅ **라벨 있음** - `sr-only` 클래스 사용
  - `<label className="sr-only" htmlFor="company-filter">거래처 필터</label>`
  - 접근성은 충족하나, 시각적 라벨 없음

### 7. 도장상태 필터
- ✅ **라벨 있음** - `sr-only` 클래스 사용
  - `<label className="sr-only" htmlFor="coating-status-filter">도장상태 필터</label>`
  - 접근성은 충족하나, 시각적 라벨 없음

---

## 🔧 버튼 및 API 확인 필요

### 버튼
- [ ] 품목 등록 버튼
- [ ] 일괄 업로드 버튼
- [ ] 템플릿 다운로드 버튼
- [ ] Excel 내보내기 버튼
- [ ] 인쇄 버튼
- [ ] 초기화 버튼
- [ ] 수정 버튼
- [ ] 삭제 버튼
- [ ] 일괄 삭제 버튼

### API 엔드포인트
- [ ] `GET /api/items` - 품목 목록 조회
- [ ] `POST /api/items` - 품목 등록
- [ ] `PUT /api/items/[id]` - 품목 수정
- [ ] `DELETE /api/items/[id]` - 품목 삭제
- [ ] `POST /api/upload/items` - 엑셀 업로드
- [ ] `GET /api/download/template/items` - 템플릿 다운로드

---

## 🔍 상세 확인 진행 중

